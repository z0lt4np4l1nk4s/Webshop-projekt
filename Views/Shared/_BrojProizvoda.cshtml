@{
    int i = 0;
    foreach (Webshop.Models.Proizvodi p in ViewBag.Proizvodi)
    {
        if (p.Kategorije.ID == Model.ID)
        {
            i++;
        }
    }
    <text>@i</text>
}
